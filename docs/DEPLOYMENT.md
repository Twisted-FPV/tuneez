# Deployment

B12 can be your marketing/documentation site. Deploy this TuneEZ app separately.

Recommended:
- FastAPI backend
- Browser frontend served by FastAPI
- Docker on Render/Railway/Fly/VPS
- Android app via Capacitor

Web Serial requires HTTPS and compatible browsers. Android direct FC extraction should use a native USB serial plugin.
